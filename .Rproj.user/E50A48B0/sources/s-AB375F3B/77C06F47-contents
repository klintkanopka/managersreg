#' @title Manager's Regression Analysis
#'
#' @description Implements the manager's regression by sorting the X_i and Y_i values independently of each other. Provides better regressions most of the time.
#'
#' @param formula an object of class "formula" (or one that can be coerced to that class): a symbolic description of the model to be fitted.
#'
#' @param data a dataframe containing the variables in the model
#'
#' @return an object of class "lm"
#'
#' @examples  managers.lm(Y ~ X, data = d)
#'
#' @export managers.lm()

managers.lm <- function(formula, data){
  d <- as.data.frame(apply(data, 2, sort))
  m <- lm(formula, d)
  return(m)
}
